

// global variable

int width, height;
png_byte color_type;
png_byte bit_depth;
png_bytep *row_pointers;



int main(int argc, char *argv[]) {
  if(argc != 3) abort();

  read_png_file(argv[1]);
  process_png_file();
  write_png_file(argv[2]);

  return 0;
}
